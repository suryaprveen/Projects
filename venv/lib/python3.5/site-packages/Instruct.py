'''

*** INSTRUCTIONS TO USE SEARCHER MODULE ***

1. First we import our MODULE-:
=> from Searcher import *

2. Then, we can call function store_data(file) with a file as argument
=> data = store_data(<Myfile.txt>)

3. Then we call to_lower(list) with a list as an argument
=> to_lower(data)

4. Then we call remove_slashn(list) with a list as an argument
=> remove_slashn(data)

** NOW ALL THE FILE MODIFICATION FUNCTIONS ARE OVER **
** Now let's search! **

***** OPTIONAL ******
5. We call compute(str) with your word as argument, we store the output in a list
=> list = compute(<myword>)

6. At last, we call letter(str,list) with your word and your list as argument
=> letter(word,data)

********** THE show.py can be used to call functions to display your messages in an interactive way **********************
Like to display a message to initialize we can call init(msg) with your message as argument
=> init('Intializing Word check')

Show(msg) is used as an error generating function
=> show('INVALID NUMBER')

'''

